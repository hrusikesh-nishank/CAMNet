# Results

This folder is optional and intended for local outputs such as:
- saved figures (triplets, comparisons)
- evaluation logs
- temporary experiment artifacts

Do not upload large files (images, logs, checkpoints) to the repository.
